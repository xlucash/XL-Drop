name: xlucashDrop
version: '${project.version}'
main: me.xlucash.xldrop.DropMain
api-version: '1.19'
commands:
  drop:
    description: Otwiera GUI lub przeladowuje plugin.
    usage: /<command> [reload]
permissions:
  drop.reload:
    description: Uprawnienie do przeładowania pluginu.
    default: op
